name: CI
on: [push, pull_request]

jobs:
  festim-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          environment-file: environment.yml
          activate-environment: festim-benchmark-env

      - name: Placeholder step for Job 1
        shell: bash -l {0}
        run: echo "Running Job 1"

  festim-1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          environment-file: environment_festim_1.yml
          activate-environment: festim-benchmark-env_festim_1

      - name: Placeholder step for Job 2
        shell: bash -l {0}
        run: echo "Running Job 2"
